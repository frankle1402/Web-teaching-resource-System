# 教学资源生成与管理系统

面向医卫类教师的教学资源生成平台（MVP版本）

## 项目简介

本系统允许教师通过 AI 和可视化编辑器创建 HTML 教学资源，管理、分类并发布到互联网进行公开访问与嵌入。

## 技术栈

### 前端
- Vue3 + Vite
- Element Plus
- TailwindCSS
- Pinia (状态管理)
- Vue Router
- Axios

### 后端
- Node.js + Express
- SQL.js (SQLite)
- JWT (认证)
- Multer (文件上传)

## 功能特性

### ✅ 已实现（阶段1）
- 用户模拟登录（手机号登录）
- 用户认证和授权
- Dashboard框架
- 基础路由和导航

### 🚧 开发中（后续阶段）
- 资源CRUD管理
- AI智能生成内容（302.ai）
- TipTap可视化编辑器
- 版本控制和历史记录
- 资源发布和公开访问
- 文件夹分类管理

## 快速开始

### 环境要求
- Node.js >= 18.0.0
- npm 或 pnpm

### 方式一：一键启动（推荐）

双击运行 `start.bat` 即可自动完成以下操作：
- 检查运行环境
- 自动安装依赖（首次运行时）
- 启动后端服务（端口：8080）
- 启动前端服务（端口：5173）
- 在浏览器中打开系统

停止服务：双击运行 `stop.bat`

### 方式二：手动启动

#### 安装依赖

```bash
# 后端
cd backend
npm install

# 前端
cd frontend
npm install
```

#### 配置环境变量

后端配置文件：`backend/.env`

```env
PORT=8080
NODE_ENV=development
DB_PATH=./database/teaching_resources.sqlite
JWT_SECRET=your_secret_key
AI_API_KEY=your_302_ai_api_key
FRONTEND_URL=http://localhost:5173
BASE_URL=http://localhost:8080
```

#### 启动服务

```bash
# 启动后端（终端1）
cd backend
npm run dev

# 启动前端（终端2）
cd frontend
npm run dev
```

访问地址：
- 前端：http://localhost:5173
- 后端：http://localhost:8080

#### 测试账号
- 手机号：`13800138000`、`13900139000` 等

## 项目结构

```
Web-teaching-resource-System/
├── backend/                 # 后端服务
│   ├── src/
│   │   ├── controllers/     # 控制器
│   │   ├── routes/          # 路由
│   │   ├── middlewares/     # 中间件
│   │   ├── database/        # 数据库
│   │   └── app.js           # 入口文件
│   ├── package.json
│   └── .env
├── frontend/                # 前端应用
│   ├── src/
│   │   ├── components/      # 组件
│   │   ├── pages/           # 页面
│   │   ├── router/          # 路由配置
│   │   ├── store/           # 状态管理
│   │   ├── api/             # API封装
│   │   └── main.js          # 入口文件
│   ├── package.json
│   └── vite.config.js
├── docs/                    # 文档
│   └── database.sql         # 数据库Schema
├── MVP/                     # 产品文档
│   └─��� 000.PRD.md           # 产品需求文档
└── README.md
```

## 数据库

系统使用 SQLite 数据库（通过 SQL.js 实现），包含以下核心表：

- `users` - 用户表
- `folders` - 文件夹表
- `templates` - 模板表
- `resources` - 教学资源表
- `resource_versions` - 资源版本历史表
- `ai_generation_logs` - AI生成记录表

数据库文件位置：`backend/database/teaching_resources.sqlite`

## API文档

### 认证接口

- `POST /api/auth/mock-login` - 模拟登录
- `POST /api/auth/logout` - 退出登录
- `GET /api/auth/verify-token` - 验证Token

更多API文档请查看后续更新。

## 开发计划

- [x] 阶段1：基础框架搭建（当前完成）
- [ ] 阶段2：资源管理模块
- [ ] 阶段3：AI生成功能
- [ ] 阶段4：编辑器集成
- [ ] 阶段5：发布与优化

## 贡献指南

欢迎提交 Issue 和 Pull Request！

## 许可证

MIT License

## 联系方式

如有问题，请通过 Issue 联系。
